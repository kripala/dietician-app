# Local Development Configuration
# DO NOT COMMIT - Contains local-only settings
# For actual local testing, copy this file to project root as application-dev.properties.local
# and use SPRING_CONFIG additional-location to load it

# Database Configuration (Local PostgreSQL)
spring.datasource.url=jdbc:postgresql://localhost:5432/dietician_db?currentSchema=diet&ssl=false
spring.datasource.username=postgres
spring.datasource.password=postgres

# JWT Secret (jumbled for commit - use real value in local file)
jwt.secret=jWEtdGVzdC1zZWNyZXQta2V5LWZvci1sb2NhbC10ZXN0aW5n

# Encryption Key (jumbled for commit - use real value in local file)
encryption.key=dGVzdC1lbmNyeXB0aW9uLWtleS1mb3ItdGVzdGlu

# Google OAuth2 (jumbled for commit - use real values in local file or environment)
# Get your credentials from https://console.cloud.google.com/apis/credentials
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:your-client-id.apps.googleusercontent.com}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:your-client-secret}
spring.security.oauth2.client.registration.google.redirect-uri=${OAUTH_REDIRECT_URI:http://localhost:8080/api/auth/oauth2/callback/google}

# Mail Configuration (MailHog for local testing)
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=test@test.com
spring.mail.password=
spring.mail.test-connection=false

# CORS (local development)
cors.allowed-origins=http://localhost:8081,http://localhost:19006

# Log level for debugging
logging.level.org.springframework.security.oauth2=TRACE
logging.level.com.dietician=DEBUG
logging.level.org.springframework.web.client=TRACE
