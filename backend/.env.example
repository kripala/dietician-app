# Backend Environment Variables Template
# Copy this file to .env and fill in your actual values

# Database Configuration
DB_USERNAME=dietician_user
DB_PASSWORD=your_secure_database_password_here

# JWT Configuration (Generate using: openssl rand -base64 32)
JWT_SECRET=your-256-bit-jwt-secret-key-change-this-in-production

# Encryption Key (Generate using: openssl rand -base64 32)
ENCRYPTION_KEY=your-256-bit-encryption-key-change-this-in-production

# Mail-in-a-Box SMTP Configuration
MAIL_HOST=box.yourdomain.com
MAIL_PORT=587
MAIL_USERNAME=noreply@yourdomain.com
MAIL_PASSWORD=your_email_password_here

# Google OAuth 2.0 Configuration
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# CORS Configuration (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:8081

# IMPORTANT SECURITY NOTES:
# 1. Never commit this file with actual values to version control
# 2. Generate strong random keys using: openssl rand -base64 32
# 3. Use different keys for development and production
# 4. Rotate keys periodically for enhanced security
# 5. Store production keys in a secure secrets manager
